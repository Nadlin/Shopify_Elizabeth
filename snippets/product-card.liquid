
    <div class="border bg-white shadow-lg rounded-lg overflow-hidden">
        <a href="{{ product.url }}" alt="{{ product.title }}">
            <div class="h-80 overflow-hidden">
                {% if product.featured_image %}
                    <img class="w-full h-full object-cover transition duration-1000 ease-in-out hover:scale-105 transform" height="{{ product.featured_image.height }}" width="{{ product.featured_image.width }}" src="{{ product.featured_image | image_url: width: 400 }}" alt="{{ product.featured_image.alt }}">
                {% else %}
                    {{ 'product-1' |  placeholder_svg_tag: 'w-full h-full object-cover bg-gray-100' }}
                {% endif %}
            </div>
        </a>
        <div class="flex flex-col p-4">
            <span class="text-sm text-gray-400">{{ product.vendor }}</span>
            <a class="text-gray-700 uppercase font-medium" href="{{ product.url }}">{{ product.title }}</a>
            <p class="text-gray-500">{{ product.price | money }}</p>
        </div>
    </div>
